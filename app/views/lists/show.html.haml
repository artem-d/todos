%p#notice= notice

%h3= link_to 'My Lists', lists_path
%span This list:
= link_to 'Destroy List', @list, :confirm => 'Are you sure?', :method => :delete
\|
= link_to 'Edit', edit_list_path(@list)

%h2= @list.name

%ul#tasks
  - @tasks.each do |task|
    %li= task.title
    .description= task.description